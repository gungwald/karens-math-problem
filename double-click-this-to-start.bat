@echo off

rem Runs the JavaScript program in the cscript interpreter.
cscript //nologo karens-math-problem.js

rem Wait for the user to press a key so the window doesn't disappear before
rem you can see the results.
pause
